import { Typography } from "@mui/material";

export const Logo = () => {
  return (
    <Typography
      variant="logo1"
      sx={{
        textAlign: "center",
      }}
    >
      Personal Financier
    </Typography>
  );
};
